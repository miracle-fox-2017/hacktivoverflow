import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

const state = {
  question: []
}

const mutations = {
}

const actions = {
}

const store = new Vuex.Store({
  state,
  actions,
  mutations
})

export default store
